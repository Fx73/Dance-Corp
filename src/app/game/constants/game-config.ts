export const CONFIG = {
    APP: {
        DEBUG_MODE: false,
    },
    GAME: {
        MAX_MISSED_FRAME_HOLD: 2,
        BEAT_PER_MEASURE: 4,
    },
    DISPLAY: {
        BEAT_INTERVAL: 150, //px
        TARGET_PERCENT: 0.1 // % from top;
    }
};
